<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BONGGOLAN — The Signature</title>
    <link href="https://fonts.googleapis.com/css2?family=Syne:wght@700;800&family=Montserrat:wght@300;500;600&display=swap" rel="stylesheet">
    <style>
        * { box-sizing: border-box; transition: all 0.4s cubic-bezier(0.25, 1, 0.5, 1); }
        :root { --bg: #0a0a0a; --gold: #d4af37; --text-white: #ffffff; --text-dim: rgba(255, 255, 255, 0.6); }
        body { font-family: 'Montserrat', sans-serif; margin: 0; background-color: var(--bg); color: var(--text-white); line-height: 1.6; overflow-x: hidden; }
        .marquee { background: var(--gold); color: var(--bg); padding: 8px 0; font-family: 'Montserrat', sans-serif; font-size: 0.75rem; font-weight: 600; display: flex; overflow: hidden; white-space: nowrap; text-transform: uppercase; }
        .marquee-inner { display: inline-block; animation: scroll 20s linear infinite; }
        @keyframes scroll { from { transform: translateX(0); } to { transform: translateX(-50%); } }
        header { height: 50vh; display: flex; flex-direction: column; justify-content: center; align-items: center; text-align: center; padding: 20px; background: radial-gradient(circle at center, #1a1a1a 0%, #0a0a0a 100%); }
        .brand { font-family: 'Syne', sans-serif; font-size: 3rem; letter-spacing: -1px; margin: 0; background: linear-gradient(to bottom, #fff 30%, var(--gold) 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; text-transform: uppercase; }
        .container { max-width: 500px; margin: 0 auto; padding: 40px 20px; }
        .card { position: relative; margin-bottom: 100px; }
        .img-box { width: 100%; height: 400px; border-radius: 15px; overflow: hidden; border: 1px solid rgba(212, 175, 55, 0.3); background: #111; }
        .product-img { width: 100%; height: 100%; object-fit: cover; }
        .overlay-info { position: absolute; bottom: -30px; right: 0; background: rgba(255, 255, 255, 0.05); backdrop-filter: blur(15px); padding: 20px; border-radius: 12px; border: 1px solid rgba(255, 255, 255, 0.1); width: 85%; z-index: 10; }
        .product-name { font-family: 'Syne', sans-serif; font-size: 1.3rem; margin: 0; color: var(--gold); }
        .btn-luxury { display: block; width: 100%; padding: 12px; background: white; color: black; text-align: center; text-decoration: none; font-weight: 700; font-size: 0.7rem; letter-spacing: 2px; text-transform: uppercase; border-radius: 5px; margin-top: 15px; }
        footer { text-align: center; padding: 60px 20px; border-top: 1px solid rgba(255,255,255,0.05); }
        .owner-name { font-family: 'Syne', sans-serif; font-size: 1rem; color: var(--gold); letter-spacing: 2px; text-transform: uppercase; }
    </style>
</head>
<body>
    <div class="marquee"><div class="marquee-inner"><span>BONGGOLAN GRESIK • THE SIGNATURE COLLECTION • BY PUTRI F2 • BONGGOLAN GRESIK • BY PUTRI F2 • </span></div></div>
    <header>
        <p style="letter-spacing: 5px; font-size: 0.6rem; color: var(--gold);">AUTHENTIC HERITAGE</p>
        <h1 class="brand">BONGGOLAN</h1>
    </header>
    <div class="container">
        <div class="card">
            <div class="img-box"><img src="snack1.png" class="product-img"></div>
            <div class="overlay-info"><h2 class="product-name">ORIGINAL</h2><a href="https://wa.me/62895358814387" class="btn-luxury">Order Now</a></div>
        </div>
        <div class="card">
            <div class="img-box"><img src="snack2.png" class="product-img"></div>
            <div class="overlay-info" style="right: auto; left: 0;"><h2 class="product-name">SPICY</h2><a href="https://wa.me/62895358814387" class="btn-luxury">Order Now</a></div>
        </div>
        <div class="card">
            <div class="img-box"><img src="snack3.png" class="product-img"></div>
            <div class="overlay-info"><h2 class="product-name">PREMIUM</h2><a href="https://wa.me/62895358814387" class="btn-luxury">Order Now</a></div>
        </div>
    </div>
    <footer>
        <div class="owner-name">PutriF2 — Fellyn </div>
    </footer>
</body>
</html>
